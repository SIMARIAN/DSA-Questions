void pushZerosToEnd(vector<int>& arr) {
        int pos=0;
        for (int i=0;i<arr.size();i++){
            if (arr[i]!=0){
                arr[pos]=arr[i];
                pos++;
            }
        }
        for (int i=pos;i<arr.size();i++){
            arr[i]=0;
        }
    }
